{
  "name": "campusqr",
  "version": "1.0.0",
  "private": true,
  "description": "CampusQR university smart QR campus management platform",
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "prettier --write .",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "seed": "turbo run seed"
  },
  "devDependencies": {
    "@campusqr/config": "workspace:*",
    "prettier": "^3.4.2",
    "turbo": "^2.3.3"
  }
}
